<%= form_with(model: home_page) do |form| %>
  <% if home_page.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(home_page.errors.count, "error") %> prohibited this home_page from being saved:</h2>

      <ul>
        <% home_page.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
